# 73 — Dashboard Revoked Cache KPI Visibility

Stand: 2026-02-11

The local dashboard security KPI section now also displays:
- `revoked_cache_hit_total`
- `revoked_cache_store_total`

This keeps cache behavior visible without requiring direct `/kpi` JSON inspection.
